# All variables are optional — the dashboard runs fully on synthetic data
# when they are unset, missing, or the upstream API rate-limits you.

# https://www.abuseipdb.com/account/api
ABUSEIPDB_API_KEY=

# https://urlhaus.abuse.ch/api/ (optional — raises rate limits)
URLHAUS_API_KEY=

# https://threatfox.abuse.ch/api/ (optional — raises rate limits)
THREATFOX_API_KEY=
